apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding

metadata:
  name: pod-reader-binding
  namespace: rbac

subjects:
- kind: ServiceAccount
  name: app-sa
  namespace: rbac

roleRef:
  kind: Role
  name: pod-reader
  apiGroup: rbac.authorization.k8s.io