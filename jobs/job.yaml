apiVersion: batch/v1
kind: Job

metadata:
  name: db-migration
  namespace: jobs

spec:
  template:
    spec:
      containers:
      - name: migration
        image: busybox:1.36
        command:
        - sh
        - -c
        - |
          echo "Starting database migration..."
          sleep 20
          echo "Database migration completed."

      restartPolicy: Never

  backoffLimit: 3